#!/usr/bin/env python3

import boto3
attrs = {
'endpoint_url': 'http://localhost:8000'}

if len(sys.argv) == 2:
    if "prod" == system.argv[1]:
        attrs = {}

dynamodb = boto3.client('dynamodb', **attrs)
